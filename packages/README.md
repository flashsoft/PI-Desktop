# packages

- `shared` — IPC / 协议契约
- `i18n` — 英文源文案目录
- `plugin-sdk` — 面向插件作者的类型与辅助工具
- `agent-runtime` — pi sidecar 与运行时封装
- `agent-host` — 无头 Agent Host 模块（准入、轮次队列、审批、事件日志）
- `host-runtime` — 独立于 Electron 的运行时层（stdio 传输、重启监督、轮次生命周期）
- `racp` — RACP-WS 服务端与客户端核心、`ws` 绑定、设备令牌配对
